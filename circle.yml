machine:
  node:
    version: 6.9
test:
  override:
    - npm run test

deployment:
  develop:
    branch: feature/deploy
    commands:
      - npm rebuild node-sass
      - npm run build
      - cd dist && aws s3 sync . s3://cumulus-dashboard
